/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
  return knex.schema.alterTable('users', (table) => {
    // Custom subtitle/description to display on the public booking page (/u/username)
    // Appears underneath "Book a hangout with {username}"
    table.text('booking_page_subtitle');
  });
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
  return knex.schema.alterTable('users', (table) => {
    table.dropColumn('booking_page_subtitle');
  });
};
